import { TAnswers } from "../types.js";
import { execCommand } from "../utils.js";

export const createReactProject = (answers: TAnswers) => {
  console.log(`Sorry! this project still under development!`);
};
